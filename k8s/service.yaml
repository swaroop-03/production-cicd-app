apiVersion: v1
kind: Service
metadata:
  name: production-cicd-app
  namespace: production
spec:
  selector:
    app: production-cicd-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
